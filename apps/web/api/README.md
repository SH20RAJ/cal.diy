# API Structure

CreatorCall APIs live under `app/api/creatorcall/*` for App Router route handlers.

This folder exists to document the architecture split:
- `app/api`: executable API routes
- `lib`: shared service utilities
- `features`: business logic
- `components`: UI implementation
